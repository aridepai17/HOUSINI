import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: `'Vanilla Extract', sans-serif`,
    body: `'Vanilla Extract', sans-serif`,
  },
});

export default theme;
